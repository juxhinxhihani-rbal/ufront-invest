import { css } from "@emotion/react";

export const styles = {
  container: css({
    gap: "80px!important",
  }),

  navigationContainer: css({
    flexShrink: 0,
  }),

  contentContainer: css({
    flexGrow: 1,
  }),
};
