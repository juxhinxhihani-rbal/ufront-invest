import { css } from "@emotion/react";

export const amlStyles = {
  tableContainer: css({ width: "100%" }),
  table: css({ overflowX: "auto" }),
  wordBreak: css({ wordBreak: "break-word", whiteSpace: "break-spaces" }),
};
