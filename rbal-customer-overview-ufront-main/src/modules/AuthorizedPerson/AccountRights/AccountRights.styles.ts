import { css } from "@emotion/react";

export const styles = {
  title: css({
    color: "#131416",
  }),
  subtitle: css({
    color: "#42444C",
  }),
  button: css({
    width: "fit-content",
    textDecoration: "none",
  }),
  buttonsWrapper: css({
    justifyContent: "space-between",
  }),
  personsInfo: css({
    display: "flex",
    alignItems: "flex-start",
    alignSelf: "stretch",
  }),
  contentWrapper: css({
    justifyContent: "space-between",
  }),
};
